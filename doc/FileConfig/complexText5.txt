# 这是注释和空行
import path array.txt array
import path dictionary.txt dic
import namespace SettingExtend

$array[0]
目前拥有的权限为：$dic["user"]
[code]
print("输出1");
print(DateTime.Now.ToString("yyyy-MM-dd"));
[/code]
结尾

[code]
var setting = Utility.Parse("Text1.txt");
print(setting.Value);
[/code]
结尾2